# Native EPR bridge checkpoint — 12 September 2026

The registration packet closes literal native coordinate calibration. It also supports a useful EPR distinction: shared classical deck history obeys the CHSH bound 2, while reflections built from the native analyzer projectors give an operator norm 2√41/5 ≈ 2.56125. Compatible tensor sectors carrying this construction embed in the actual native G1800 quotients. Preparation, measurement admissibility, and outcome probabilities remain open.

## Registration and WXYZTI boundary

The uploaded native generator bundle has exactly the previously tested 120 G60 edges and named a, b, ab permutations. The 201H808C canonical fifteen fibers and projection agree with their V4 orbits. This closes the earlier uncertainty about literal native labels.

Alignment 012 and block derivation 021 still use C endpoints without deriving a C-to-native chart. Audit 121 supplies aggregate reused-pair incidence, not a transitionwise two-layer law or arrival-sheet selector. Consequently the WXYZTI chart/chooser branch remains open; it is not required for the native analyzer calculation below. No historical producer was executed and no user's repository was modified.

## Classical shared-history control

We proposed two abstract copies of a native lifted-edge fiber, with uniform shared deck element g in {I,a,b,ab}. Left receives g·e and right receives b·g·e. This is a declared model preparation, not derived native source dynamics or physical spatial separation. Every prepared state is an actual native edge, and all project to the same oriented G15 edge.

With two binary settings per observer, each observer has 256 deterministic local tables on four local states. All 65,536 joint tables were checked exactly. For S = E00 + E01 + E10 − E11:

| S | Number of models |
|---|---:|
| −2 | 4,096 |
| −1 | 16,384 |
| 0 | 24,576 |
| 1 | 16,384 |
| 2 | 4,096 |

All 1,296 balanced-marginal models and all 256 models with perfect same-setting anticorrelation also obey |S| ≤ 2. There were 524,288 exact marginal checks.

The pointwise expression A0(B0+B1)+A1(B0−B1) is always ±2. Therefore arbitrary common setting-independent weights, extra classical hidden history, and local stochastic mixtures preserve the bound. Assumptions are local response, setting-independent preparation, a common ensemble, and no setting-dependent postselection. This is a native regression control, not a new Bell theorem or a refutation of the framework.

## Native analyzer operator

The existing native projector data provide two real three-dimensional G60 modes at eigenvalue 1+√5, with deck characters (a,b) = (−1,+1) and (−1,−1). Each has six rank-one order-five subgroup analyzer projectors. Exact arithmetic verifies the projectors, three independent spanning vectors, and squared pair overlap 1/5 for all fifteen distinct pairs in each sector.

Choose binary reflections Oi = 2Πi − I on a mode. Distinct choices do not commute; their commutator norm is 8/5. A canonical pair is

Z = diag(1,−1,−1),

R = [[−3/5,4/5,0],[4/5,3/5,0],[0,0,−1]].

The tensor operator C = Z⊗(Z+R) + R⊗(Z−R) has exact characteristic polynomial

(t−2)^3(t+2)^2(t²−36/25)(t²−164/25).

Hence ||C|| = 2√41/5 > 2. Pair-overlap equivalence covers all distinct analyzer-pair choices in either sector. This is operator capacity. Interpreting these reflections as admissible measurement instruments and their expectations as experimental correlations requires additional justification.

## Placement in actual native G1800

For character modes f and g, their product descends through the diagonal deck quotient by B exactly when χf(B)χg(B)=+1. The normalized map is J(f⊗g)([u,v])=√2 f(u)g(v).

| Factor choices | Quotient kernels admitting descent | Number of nine-dimensional sectors |
|---|---|---:|
| Same character on both factors | a, b, ab | 6 |
| Opposite characters | a | 2 |

All eight compatible sectors were checked against the actual 1,800-vertex adjacency lists: 129,600 feature equations at eigenvalue 2+2√5, fiber independence, and exact quotient/product Gram identities. The four incompatible choices reverse sign across a quotient fiber and were rejected. Factor reflections preserve their character spaces and thus act on the compatible embedded sectors.

This establishes a native carrier for the candidate operator. It does not establish physically separated subsystems, graph-local instruments, preparation dynamics, Born weighting, or covariance with every native history/frame operation. No identification with the earlier E⊗W representation having norm 2√2 is asserted.

## Next EPR target

The next decisive gate is a native preparation-and-readout construction. It must specify a setting-independent source state, justify the available binary instruments, and produce normalized joint outcome weights from the permitted mechanics. Those weights must then be checked for remote-setting-independent marginals and the resulting CHSH value. Selecting a maximal-eigenvalue state and imposing Born weights would demonstrate a conditional model, not derive this gate.

WXYZTI chooser work becomes relevant here only if it supplies an admissible source, instrument, or weight rule. Further coordinate collection without one of those connections would not advance this EPR target.

## Reproduction and provenance

The accompanying archive contains all three audit scripts, exact arithmetic support, input snapshots, JSON results, and SHA-256 receipts. From its extracted directory run:

```sh
python3 verify.py
python3 operator_gate.py
python3 native_tensor_gate.py
```

The first two use the Python standard library; the third also requires NumPy. Inputs include the uploaded native generator bundle and 201H808C projection, previous native deck/quotient actions, and the previously derived native analyzer projectors. The audit consumes these snapshots; it does not replay their historical producers. Registration source files are retained for the bounded provenance findings above.
