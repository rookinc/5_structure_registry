\partstart{Measure, Registration, and Quantum Readout}
\section{Measure, Frequency, and Provenance}\label{sec:measure}
Measure assigns weights $w_o^c$ to outcomes in a context. Frequency realization asks whether registered counts $N_o^c(m)/m$ approach those weights. Process provenance specifies the law generating the chronology. Apparatus provenance supplies the physical setting and outcome interface. A result at one interface is not a substitute for the others.

The supplied reclosure report records a finite route from registered-history overlap to the Born weights and a measure-preserving four-cell registration. It retains two external fronts: native nonperiodic growing-history provenance and physical face-instrument realization \citep{measurereclosure}.

\section{Registered Measure}\label{sec:registration}
Registered measure is a native relational measure realized through an admitted output transducer without replacing the intended observable. A coarse readout can faithfully realize its specified coarse observable while differing from the original fine-grained distribution.
\input{statements/S25_1}
The quantum analogue is contractivity under a common channel \citep{watrous2018}. A changed instrument or side information is an additional resource. The sampled face family in the reclosure report lacks the intended state-independent CHSH contrast; a frequency scheduler cannot supply a distinction absent from that upstream interface \citep{measurereclosure}.

\section{Probability as History Readout}\label{sec:frequency}
\input{statements/S26_1}
An extensible, operationally accessible history, an external drive, or a stochastic law changes the complete-state premise. A passive record that never affects the controller does not.

For a supplied weight $0<\alpha<1$ and $0\leq\theta<1$, the sequence
\input{equations/E33}
has $b_n\in\{0,1\}$ and telescoping count
\[
N_1(N)=\lfloor N\alpha+\theta\rfloor-\lfloor\theta\rfloor,
\qquad |N_1(N)-N\alpha|<1.
\]
This proves realization, including irrational limiting weights, once the weight and update are supplied. It does not select the native source or establish independent trials. Context-local counters preserve visit-count bounds under interleaving. The stronger full four-cell construction, with event-ready herald ordering, is imported from the reclosure report rather than inferred from this binary example \citep{measurereclosure}.

\section{The Born Correspondence Program}\label{sec:born}
\subsection*{History overlap and receipt grading}
The supplied twelve registered pentagons have signed incidence $B_{\rm hist}$ of size $30\times12$ and
\input{equations/E34}
The new package reconstructs all twelve pentagons from its local $G_{15}$ presentation and supplies their signed incidence matrix. It checks signed-permutation equivalence of the resulting Gram to the supplied $M_{\rm hist}$. This extends reproducibility of the matrix construction without replacing the imported native history and receipt designations \citep{historyconference}.

Let $Z$ be $+1$ on the six $b$-receipt cycles and $-1$ on the six $ab$-receipt cycles. Then
\input{equations/E35}
Writing $J=K_{\rm hist}/\sqrt5$ gives
\input{equations/E36}
Thus receipt grading and the history operator furnish a real linear complex structure on the coefficient space \citep{historyclifford}. In the Mode vocabulary, $b/ab$ are orientation states, $Z$ is a diagonal grading, and $\Mevt$ is a frame-reversing action. The new modal check is on relative closure frames, not an assertion that $Z=\Mevt$ on this twelve-dimensional space.

\subsection*{Spectral masses and the six-axis kernel}
The positive matrix
\input{equations/E37}
has trace one and history-operator eigenvalues $\pm\sqrt5$, each with multiplicity six. Hence
\input{equations/E38}
The native-orbit justification of the equal cycle average remains imported; the spectrum and normalization are checked exactly \citep{historyclifford}.

The symmetric six-axis conference matrix $C$ has zero diagonal, off-diagonal entries $\pm1$, and $C^2=5I_6$. Therefore
\input{equations/E39}
is positive semidefinite, with three eigenvalues two and three zero. Its unit diagonal makes it a Gram kernel for six unit vectors $\mathbf n_i\in\R^3$. Orthogonal coordinate choices leave the kernel unchanged.

\subsection*{Antisymmetric preparation and the complete table}
Take $A_i=\mathbf n_i\cdot\boldsymbol\sigma$ on $\C^2$, let $S$ exchange the two tensor factors, and set $P_-=(I-S)/2$. This projector has rank and trace one. For traceless $A,B$,
\input{equations/E40}
The trace identity $\Tr[S(A\otimes B)]=\Tr(AB)$ gives the second equality. With $\Pi_{a|i}=(I+aA_i)/2$,
\input{equations/E42}
For distinct axes, the cells are $(5\pm\sqrt5)/20$ and their parity sums give \eqref{eq:bornmasses}. Equal settings instead give exact anticorrelation; the off-diagonal golden cells do not apply there.

This is a derivation in the specified quantum representation using tensor composition, Pauli observables, antisymmetric preparation, and the trace rule. The native-history route supplies finite provenance for the corresponding weights. The package verifies all 36 ordered setting pairs, normalization, positivity, balanced marginals, and equal-setting anticorrelation. Physical analyzer realization and chronological source provenance retain their own status.

\input{sections/05A_signed_axis_reference}
