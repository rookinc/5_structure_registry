\partstart{Propagation and the Native Causal Datum}
\section{Event-Preserving Histories}\label{sec:eventpreserving}
\input{statements/S20_1}
The preserved object is a specified relational payload, encoding, or invariant; successive handoffs remain distinct occurrences. Write $\mathcal C_{\rm event}$ for histories satisfying input/output compatibility, local admission, adjacent address change, and complete-output recovery. A bare carrier walk need not satisfy these requirements. The subscripted history-domain symbol $\mathcal C_{\rm event}$ is distinct from the scalar functional $\mathcal C(R)$.


Internal conservative evolution and field-address propagation are different operations. Reorienting a rotor register or locally coupling two field components does not itself establish an encoded payload handoff. The gyroscopic and Maxwell comparisons in Sections~\ref{sec:gyroscopic}--\ref{sec:maxwelltranslation} therefore leave event counting, the field-address domain, and $\cth=1$ unchanged.

\section{Relational Field Distance}\label{sec:fielddistance}
The field graph $\mathcal F$ has transducer addresses as vertices and admitted field adjacencies as edges. Its local internal carrier may be $G_{60}$. These are different levels of description. For the locality theorem, $d_{\rm field}$ is unweighted shortest-path distance. A history $\gamma$ has source $s(\gamma)$, target $t(\gamma)$, and positive irreducible depth $|\gamma|_{\rm event}$ in the specified grammar. Directed, weighted, or state-dependent variants require a corresponding distance law.

\section{The Causal Slope of Becoming}\label{sec:causalslope}
\input{statements/S22_1}
Its preferred readings are ``one event per event'' and ``one transmission per relationship per event,'' with relationship meaning an admitted adjacent handoff.

\input{statements/S22_2}
The upper bound and its existence witness have different roles. Saturation requires an event-preserving one-step transmission, not merely a graph edge. The theorem neither selects the local admission law nor proves saturation along every long geodesic. Information-preserving admissibility is part of the domain, while locality supplies the distance estimate.

A comparison with the SI speed of light requires field-length and clock calibrations \citep{bipm}. Its denominator remains event depth; replacing it with signed winding would change both domain and mathematical type. Mode likewise adds no duration or field displacement to a handoff.

\input{statements/S22_3_cover}

\section{Transport Cocycles and Long Histories}\label{sec:cocycles}
\input{statements/S23_1}
Sustained macroscopic transport consequently requires an extended address space or an accumulated displacement register rather than bounded endpoint distance alone. In a common abelian register,
\input{equations/E31}
while changes of frame can require
\input{equations/E32}
with a specified action $\rho$. A positive word cost is additive before cancellation; a minimal cost on equivalence classes may be only subadditive. The long-history readout $\Xi(\gamma)/\ell(\gamma)$ is distinct from endpoint distance per depth and from $\tth$.
