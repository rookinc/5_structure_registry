\partstart{Quantum and Geometric Reductions}
\section{Quantum Mechanics as Internal Transducer Dynamics}\label{sec:quantum}
The proposed quantum reduction retains internal alternatives, phases, composition, and instrument readout. A coherent representation should carry admitted compositions to compositions and preserve their joint distinction structure, including reference correlations:
\input{equations/E43}
Tracing out the receipt produces the transmitted channel. A permutation can always be represented unitarily, but a full reduction also needs an admitted observable algebra, preparation rule, tensor composition, and update law. The finite $Z,J$ algebra supplies one concrete internal structure rather than all of these ingredients at once.

Interference depends on which records the apparatus distinguishes. For example,
\input{equations/E44}
has a transmitted off-diagonal term proportional to $\alpha\overline\beta\langle r_1|r_0\rangle$. Orthogonal receipts remove that term from the marginal; identical receipts preserve it. The complete state can remain coherent in either description.

\input{statements/S28_1}
In the stated pure-receipt form, the proof fixes a common receipt vector across the input family. This is the relevant no-information-without-disturbance restriction \citep{watrous2018,wootterszurek1982}.

Reversible premeasurement and stable outcome registration can be represented within the joint architecture. An isometry alone does not specify an outcome-selection law. The reduction must separately define how its registered outcome sequence is generated.

\section{Entanglement and Joint Relational Structure}\label{sec:entanglement}
The joint antisymmetric preparation precedes local settings. Alice's and Bob's operators act as $A_x\otimes I$ and $I\otimes B_y$. Table-level no-signaling follows by summing \eqref{eq:fourcell}: both marginals are $1/2$, independent of the remote setting. Correlation itself is not an adjacent field handoff.

Bell-local factorization is a stronger assumption:
\input{equations/E45}
For the corresponding conditional means in $[-1,1]$,
\[
|A_0(B_0+B_1)+A_1(B_0-B_1)|
\leq |B_0+B_1|+|B_0-B_1|\leq2.
\]
Averaging gives the CHSH bound \citep{chsh1969}. The QR target is therefore an explicit source-and-instrument construction with the required joint table and disclosed dependencies on settings and memory. The native admission grammar, event-ready ordering, no-signaling table, and physical apparatus are separate checks.
