# History / triality / frame realization test

This packet checks an exact abstract realization of the registered-history carrier

    Gamma = <r,s,z | r^4=s^2=z^2=1, srs=r^-1, [z,r]=[z,s]=1> ~= D8 x C2

on the four-real-dimensional mixed carrier V*=E tensor W.

Constructed map:

    r -> J_E tensor I_W
    s -> D_E tensor I_W
    z -> I_E tensor F_W

It verifies all presentation relations, image order 16, the exact H0 stabilizer,
the four-surface orbit, and the square-involution alternatives. It also crosschecks
the previous X12 signed-transfer result, whose two weight-18 minimum prisms restrict
as +/-4(I tensor F).

Boundary: this is an exact representation-level compatibility theorem for the
displayed finite structures. It is NOT a native common-domain action/provenance
certificate and is NOT a physical phase or hbar derivation.
